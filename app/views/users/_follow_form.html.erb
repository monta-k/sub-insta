<% unless current_user == @user %>
  <div id="follow_form" class="float-left follow-btn">
    <% if current_user.following?(@user) %>
      <%= render "unfollow" %>
    <% else %>
      <%= render "follow" %>
    <% end %>
  </div>
<% end %>
